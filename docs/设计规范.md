# 设计规范（基础框架）

2026-09-14量价分析沿用公共section-tabs，页签“周分析 / 月分析”；查询仅保留当前分析周或月份、分析品类、重置。周选择复用可搜索顶层浮层，回显完整起止日期；月选择复用form-control年月控件。月份清空使用公共query-control--date与query-clear，无新增字号或配色；本页隐藏字段使用限定analysis-query的hidden规则。周显示本期指标及明细，月图表按统计片段展示并标明首尾天数，明细依据继续页内查看。详见`市场量价分析周月维度优化记录.md`，未浏览器渲染验证。

2026-09-11长内容查看规范：`openDetailPage`与编辑共用多层工作台，顶栏按层级显示返回列表/返回上一页。纯阅读不设底部取消栏、无侧栏时不预留第二列；长正文、数据表和权限树占全宽，元数据三列紧凑排列，窄屏两列/单列。资讯原文与展示稿、版本及追溯前后内容使用公共对照区。导入预览及角色权限树解除弹窗高度限制；获取/生成记录按记录标识保留展开状态。所有页面沿用现有主题、字体、按钮及导航，不增加菜单。

2026-09-11编辑左右布局修正：三处长文本编辑使用公共工作台的`mode: 'edit'`。桌面主区与参考区约1.6:1，行高由编辑主区决定，参考全文在等高侧栏内滚动，不以参考长度撑高页面。摘要紧凑展示，分析、建议及文章获得更大的视口相关初始编辑高度；仍可垂直调整。1180px以下改为单列，参考栏恢复自然内容高度。本条仅覆盖编辑布局，复核页维持原有布局。

2026-09-11长内容工作台：需要持续阅读、长文本编辑或对照依据后复核的任务，不再使用`app-dialog`承载。公共`openTaskWorkspace`在当前模块内容区打开二级工作台，列表DOM暂时隐藏，返回后恢复页签、筛选、页码、滚动及触发焦点；浏览器返回可退出，存在未保存输入时需确认。顶部任务标题、版本状态及元数据紧凑展示，底部操作固定；桌面采用主区加320px以上侧栏，1180px以下改为单列。资讯编辑/核验以原文和展示稿对照，简报与报告编辑/复核在主区显示正文、侧栏显示保存前数据依据或复核表单。正文初始高度按摘要、分析、建议、文章分别设置，不再沿用公共96px文本域。发布确认、删除、状态切换和短表单继续使用原生dialog。本轮不建立新颜色、字号、按钮或导航体系，未浏览器渲染验证。

2026-09-11周/月展示调整：两个管理端报告页面均复用公共section-tabs，顺序为周报/月报，默认周报；页签下为统一tabpanel，保留原顶部框体但只显示当前类型一行，继续使用既有网格、字号及按钮。支持点击、左右方向键、Home/End、焦点和aria-selected关联；切换保留各自查询及分页，顶部设置和记录对应当前类型。此条替代下文同时展示两行的要求，未做浏览器渲染对照。

2026-09-10周月报告统一：商户经营报告和行情简报复用同一两行顶部、对齐列、分开的周/月配置和记录弹窗。启用使用40×24px开关、18px滑块、主题色、键盘焦点环及已启用/已停用文字，保存后生效；配置仅显示本类执行日，数据范围只读。新经营周报/月报采用完整周/自然月，旧周数数据仅历史留档，不再提供统计周数选择。未浏览器渲染对照。

2026-09-10简报设置表单重整：周报/月报采用相同字段顺序，启用状态、固定周期、执行日和时间、只读数据范围、折叠品类。执行日和时间同排，其余占整行；范围使用公共背景色、控件高度和圆角。周报只出现周几，月报只出现日期，月末规则说明单独占整行；generation-config内hidden明确不显示。未浏览器渲染验证。

2026-09-10简报顶部对齐补充：周报、月报两行共享父网格列宽，行内使用subgrid，标签纵向对齐，保留各一行和40px行高。默认周报执行中、月报等待数据，沿用现有状态标签与字号。仅静态检查，未浏览器对照。

2026-09-10小程序登录展示修正：共用底部弹层在showModal前清除top/right定位，明确贴合手机框底边，不受原生dialog默认顶部定位影响。登录页一键按钮上方和授权框共用居中脱敏号码，前三后四、中间四星；号码沿用24px主标题令牌，“本机号码”沿用12px辅助令牌。短信输入不限制内容和长度，保留非空提示；不显示验证码有效期/错误次数，发送操作保留60秒倒计时。未浏览器或字号渲染验收。

2026-09-10小程序登录补充：复用430px预览外框与公共蓝白主题、字体令牌；独立登录状态不显示底部导航。主按钮为本机号码一键登录，短信登录作为次入口，同页切换手机号/验证码表单；主要触控高度48px，窄屏验证码行可换行。授权和退出使用公共底部弹层，“我的”资料下方为图标加文字退出按钮。`merchant-login.css`只补充本次布局，不引入第二套主题。未浏览器或渲染字号验收。

2026-09-10部门路径与选中状态补充（优先于下文）：选中部门去掉左侧inset蓝色框线，仅保留蓝色字体、浅蓝背景及右侧勾选；部门图标和键盘焦点反馈保留。选择控件、浮层已选栏、用户列表、资料查看统一显示“公司 / 中心 / 部门”完整路径，使用公共字号，长路径可换行，不仅放在title提示中。浮层仍为独立顶层面板，展开不占布局。

2026-09-10市场行情简报顶部最新布局：保留公共panel，周报/月报各一行，依次横排类型、执行周期、下次执行、状态、数据范围、对应设置及记录。标签和值均复用13px控件字号，标签用辅助色，每行40px，不分层、不换行，窄空间框内横向滚动。下次时间精确到分钟，运行状态显示执行中，详细原因留在悬停及记录。复用图标文字按钮，独立设置和记录弹窗保持。未浏览器渲染或字号对照。本条替代此前60px行高及标签和值分层的布局。

2026-09-10部门树美化：`treeSelect`使用原生Popover顶层浮层及fixed定位，36px按钮触发，宽度至少360px并受视口约束，高度按上下可用空间计算，贴控件下沿或向上翻转，不占表单布局。浮层复用13px控件字体、12px辅助文字、蓝白主题和公共圆角，分为组织标题/搜索/独立滚动树/已选与清空四区。公司、中心、部门采用公共SVG图标，中心图标浅蓝底，叶节点选中为浅蓝底/左侧细线/勾选；支持父路径搜索、无匹配、折叠/展开、键盘焦点及降低动态效果。仅Node和静态检查，未浏览器或字号验收。本条替代此前面板按表单内展开的设计。

2026-09-10市场行情简报配置布局优化：列表上方以单行右对齐普通按钮提供周报设置、月报设置、生成记录，不展示完整状态卡片。周/月报采用各自的小型设置弹窗，字段顺序为启用、生成日/时间、只读数据期间、折叠品类；执行状态和历史移入独立宽弹窗，沿用公共页签切换周报/月报。商户报告继续使用原自动生成安排。复用公共按钮、dialog、section-tabs、choice-field和字号令牌，未浏览器或字号渲染验证。

2026-09-10用户资料优化：用户列表账号/姓名分列并增加邮箱/部门，继续使用公共表格及横向滚动容器，仅为用户表补充必要列宽。新增/编辑和右上角个人资料共用资料字段与校验，复用双列`edit-grid`、`detail-grid`、dialog和图标加文字按钮。所属角色按用户明确要求取消搜索：查询区原生select接入单项清空，编辑区直接复选；角色管理权限搜索保持。右上角不展示权限明细、更新时间、资料版本。未浏览器布局或字号验收。

2026-09-10简报/报告更新：两页共用“自动生成安排”与“生成设置”，复用公共panel、dialog、edit-grid、状态标签和图标文字按钮；品类沿用分组搜索多选，每月日期用1—31数值输入，避免超10项普通下拉。简报周/月报分别配置，报告按户统一周期。生成结果、下次时间和待补任务在共用状态区展示；移除手动生成与商户预览。新增布局仅使用既有令牌，保留现有查询控件及单项清空；未浏览器或字号渲染验证。见[简报与商户报告定期生成及检查记录](简报与商户报告定期生成及检查记录.md)。

2026-09-10 U08查询控件：管理端查询表单显式调用公共`enhanceQueryControls(form)`，有值时悬停/键盘聚焦显示右侧14px关闭图标、24px点击区域，沿用36px控件及公共颜色/焦点样式；无悬停能力时有值即显示。文字、清空、下拉箭头与日历入口分别预留空间。清空图标为用户确认的框内纯图标操作，含“清空+字段名”的title和无障碍名称，其余功能按钮继续图标加文字。普通及搜索下拉清回全部，独立日期只清自身；文字清空不自动查询。分析必需项清空后提示补选。编辑表单、面板内搜索、分页及商户端不自动接入。详见[管理端查询控件单项清空与检查记录](管理端查询控件单项清空与检查记录.md)，未浏览器渲染或字号验收。

2026-09-10 U06行业资讯更新：菜单“外部数据源”改为“行业资讯”，复用公共页签并按“资讯管理、来源管理”排序，默认资讯管理。分类与发布状态复用查询选择控件，三类无需搜索；列表分类复用badge，继续分列核验与发布状态。整理展示稿增加必填资讯分类，发布弹窗和详情明确发布位置，原文时间与平台发布时间分开。公共newsTypes提供双端分类字典；不新增CSS、字号或导航体系。详见[行业资讯分类与双端对应优化](行业资讯分类与双端对应优化.md)。

2026-09-10管理端内容标题区调整：用户确认所有9个管理页面移除内容区顶部重复的页面大标题、说明文字和该块间距，公共`shell.js`直接渲染业务内容容器。保留左侧菜单、顶部路径及用户区、页面内部必要的区块和弹窗标题；公共内容边距保持。菜单切换及弹窗关闭后的备用焦点使用具名主内容区，商户端详情返回顶栏不变。仅静态及Node检查，未浏览器渲染验证。

2026-09-10 U05外部数据源更新：同菜单内容切换采用公共`.section-tabs`/`.section-tab`下划线页签，含选中、焦点、键盘与ARIA关联，沿用主题令牌；操作按钮继续图标加文字。来源/核验列表使用原表格组件，资讯增加两行摘要及独立发布状态，长正文按小标题分段，原文使用可折叠留档。表单提示只说明业务字段与实际操作结果，不显示技术连接串或重复说明。详情见[外部数据源体验优化与检查记录](外部数据源体验优化与检查记录.md)。未做渲染字号对照。

2026-09-10 U04小程序更新：一级页面直接展示内容与筛选，不显示品牌/页面标题顶栏及重复列表标题；“我的”不重复底部模块入口和商户名称字段。沿用项目令牌和430px预览外框。框内搜索图标或键盘搜索提交，选择条件即查询；筛选以文字加下箭头打开底部面板，仅选中项显示勾选。移除清除筛选、刷新、加载更多及卡片内查看按钮；下拉刷新、触底自动追加，初次骨架和轻量状态反馈。详情以连续正文、摘要/指标及折叠明细组织，显示返回顶栏并隐藏五项底部导航；同版内容和引用简报按历史逐层返回。来源、口径、AI标识和版本等仍完整可查。此条替代下文按钮式加载和重复入口说明；未做浏览器或渲染字号验证。

2026-09-10 U03多品类更新：12类/5分组共用`category-catalog.js`。管理端超过10项的品类单选复用`ui.js`搜索选择器，多选支持分组搜索及已选数量；小程序品类使用带搜索的底部选择面板。全部品类分析以汇总表展示，单品类查看趋势；多品类简报详情可折叠。继续使用原令牌、字体及公共组件，未做渲染字号对照。详见[多品类扩充与检查记录](多品类扩充与检查记录.md)，下文三类普通下拉是历史实现。

2026-09-10 U02商户外框：桌面预览维持430px宽度上限，外围16px留白，使用项目边框、12px圆角和轻阴影；内容区独立滚动，顶栏和五项底部导航固定在外框布局中。宽度不超过462px时满屏适配。弹层按外框内缘定位，不延伸到桌面背景。“我的”沿用同一资料卡、图标、文字层级与间距；本户身份元数据统一来自`merchant-profile.js`。

2026-09-10商户端更新：下文T版共用报表/两列筛选/传统分页是历史方案，商户当前使用`merchant.css`及`merchant-ui.js`公共组件，详见`小程序体验优化与检查记录.md`。继承项目字号与颜色，触控区域至少44px；顶栏精简、底部导航固定并预留安全区，分类切换与底部选择面板替代后台筛选表单，加载更多替代翻页，详情使用分区和可展开的数据卡片。商户图表按实际CSS宽度计算坐标，字号不随图表缩放。管理端原组件保持原实现。

## 当前界面文案要求（2026-09-10）

按真实业务系统呈现页面与数据，不在用户界面展示演示、静态原型、虚构样例、规则模拟等说明，也不展示浏览器缓存、文件直开、刷新恢复样例等实现细节。该规则覆盖登录页、管理端、小程序端、弹窗、历史记录及生成内容，优先于旧版文档的界面标记要求。示例数据仍为虚构，开发文档如实说明实现范围。AI标识、审核状态、数据期间、来源、单位及必要的统计和经营参考说明继续保留。

U01补充（2026-09-10）：公共顶栏已移除机构/角色选择与重复说明，右侧使用头像/当前姓名/用户菜单。系统管理两页复用公共表格、查询、分页、表单与dialog。system.css仅补充账号弹层、可搜索角色多选及分组权限复选布局；正文/表格/表单字号继续沿用令牌，系统管理员以固定全功能身份展示。D版末尾关于角色切换入口的说明不再适用。

本项目是全新空目录，没有可继承的本项目页面或现成 UI 参考。需求截图是文本表格，不作视觉稿。当前建立统一蓝白中性基线，后续以用户指定风格为准，集中修改令牌。

| 项目 | 基线 |
| --- | --- |
| 主色 | #2563EB |
| 页面背景 / 表面 | #F5F7FB / #FFFFFF |
| 正文 / 辅助文字 | #1E293B / #64748B |
| 字体 | Microsoft YaHei、PingFang SC、系统无衬线 |
| 主标题 / 区块标题 | 24px / 18px |
| 正文 / 控件 / 辅助 | 14px / 13px / 12px |
| 间距 | 4、8、12、16、24、32px |
| 控件 / 卡片圆角 | 8px / 12px |
| 控件基本高度 | 36px |
| 管理端结构 | 左侧公共导航 + 顶栏 + 内容区 |
| 商户端结构 | 移动内容宽度最多 430px + 公共顶栏 + 底部四项导航 |

`tokens.css` 管理令牌，`common.css` 管理同类组件。菜单元数据位于 `config.js`，通过 `shell.js` 渲染。图标使用项目内联 SVG，由 `common.js` 统一管理，不依赖网络字体或 CDN。

框架阶段只提供入口、导航、状态标签、占位卡片等必要基础。表格、表单、弹窗、下拉等在首个真实使用场景按统一规范扩展，不提前创建无用途的组件或功能。按钮使用图标加文字，交互提供 hover 和键盘焦点反馈，尊重 reduced-motion 设置。

静态原型默认不启服务、不浏览器测试；若后续用户明确要求视觉验证，再进行同视口同缩放验证，不用截图物理像素直接反推字号。

## T01 公共组件补充

维护类页面复用 `content-stack`、`panel`、`filter-form`、`form-field`、`form-control`、`button--primary`、`data-table`、`pagination` 和 `empty-state`。查询与分页按钮和控件统一为 36px 高。表格与控件为 13px，区块标题 18px，次要日期与状态标签 12px。

语义状态颜色集中在 `tokens.css`：绿色表示已复核、黄色表示待复核、红色表示需修改，同时显示文字与圆点，不仅依赖颜色区分。窄视口允许查询区换行，表格在自身区域横向滚动，不缩放整页。公共组件仅随当前页面需要补充，后续维护类页面直接复用。

## T02 公共组件补充

`ui.js` 提供 `openDialog` 与 `searchSelect`。弹窗使用原生 dialog，明确正文 14px、标题 18px、表单和按钮 13px；主体独立滚动，头尾保留；关闭支持按钮与 Escape，并恢复触发点焦点。常规弹窗宽 620px，含导入表格或历史记录时使用 1040px 宽型，均受视口约束。

可搜索选择器使用公共控件高度和字体，弹层显示关键词输入、可选项和空状态，支持 Escape、焦点离开关闭。选中后立即发出查询条件变更。列表操作列固定在表格右侧，维护、详情、复核按钮均使用公共图标加文字。文件选择按钮同样采用图标加文字。

## T03 品类映射复用

映射页面完整复用既有查询、表格、状态标签、分页和弹窗样式，未新增独立 CSS。已映射使用绿色状态标签，未映射使用黄色状态标签。3 个分析品类为平级选项，沿用普通下拉；原始名称由业务数据预置，在列表行内建立或调整映射，不增加商品主数据维护入口。

## T04 外部数据源复用

同一模块内数据源/信息核验视图使用公共按钮组及选中状态，来源和信息分别使用既有表格、查询区、分页、状态标签和 dialog。3 个适用品类使用可换行 checkbox 组，新增公共 `choice-field`、`check-group`、`check-choice`，沿用 13px 字号与 36px 控件基线。行内操作复用文字按钮；来源地址在详情中以可换行文本展示。

## T05 分析页面

分析页局部样式在 `assets/css/analysis.css`，继续使用公共令牌、查询、卡片、表格和弹窗。指标值24px、标题18px、正文14px、统计说明13px、轴标签12px；四列指标在较窄视口降为两列或单列。价格与数量分图，颜色沿用主色/成功色/提示色作为序列色，不直接暗示涨跌利弊。缺失数据断线，点位支持鼠标与键盘查看明细。

SVG根据容器宽度重算坐标保持字号，窄区域允许图表局部横向滚动；不通过页面zoom或transform缩放修正字体。统计口径、基期范围及数据不足原因与指标同页展示。

## T06 简报与商户市场行情

复用同一 `bulletin-view.js` 正文：AI 标识和期间、摘要、分品类四项指标、价格与库存趋势、数据明细、信号、来源和复核发布元信息。管理端外层继续使用公共查询、表格和 dialog；移动端使用公共顶部及底部导航，内容采用卡片。指标桌面四列、商户两列，图表桌面双列、商户单列；标题及指标值 18px、正文 14px、操作 13px、辅助 12px。

简报 SVG 使用固定 320 CSS 像素宽度和 12px 轴标签，窄于图表时仅图表容器横向滚动，不随容器缩放字号。量价明细表可展开且独立横向滚动。生成进度使用原生 progress，错误和保存信息通过状态区域反馈；功能按钮统一图标加文字。未开展浏览器字号或视觉验收。

## T07 管理端经营报告

复用简报正文、分区、指标及版本样式，没有新增CSS或菜单。列表采用公共文字按钮和固定操作列；标题及指标18px，正文14px，表格/表单/按钮13px，辅助12px。品类占比用原生progress配合数值，周度和原始名称明细表局部横向滚动。复核弹窗内展开报告与建议全文，两项勾选确认分别对应数据口径和建议依据，使用同一复核结论及意见。

## T08 商户经营报告与建议

复用商户公共导航、market-card卡片、market-filter筛选、merchant-market下的简报正文和两列指标；表格仍为局部横向滚动。仅增加列表摘要最多4行的样式，正文详情不截断。未读/更新提示使用已有黄色状态标签，新版提示使用notice；“返回列表”“查看同版报告/建议”“查看最新版本”和“刷新内容”均为公共图标加文字按钮。商户身份仅显示，不增加账号切换导航。

## T09 行业资讯

复用现有商户卡片、4行摘要、查询表单、正文分区和分页，没有新增CSS或菜单。资讯按类型与品类筛选，标题与来源文字采用公共字号和转义；详情区分原文时间、核验时间和平台发布时间，来源/信息/发布版本分别标示。核验与发布状态在管理端既有信息表同列展示，发布弹窗和预览复用公共dialog。所有按钮使用公共图标加文字，正文不截断，来源地址以可换行文本显示。内容为虚构来源信息文本，未AI改写。

## T10 统一追溯

继续使用公共查询区、表格、固定操作列、分页和宽型dialog，不增加CSS或独立菜单。日期标签明确为操作日期；表格突出关联内容，编号、版本和商户作为辅助信息。详情使用detail-grid展示人员、时间和业务期间，前后留存按同类字段分别展示，长内容换行；来源依据使用可展开区域，避免淹没主要信息。模块6项、操作类型8项，均少于10项，无目录树。查看关联版本复用现有简报/报告详情，预置记录明确提示为演示样例。


## D版深化复用

跨年对照、本户基期、来源留档及质量报告复用既有panel/table/details/dialog，不建立新字号或颜色体系。机构/角色入口放在公共shell顶栏，使用已有form-control/page-size；顶栏允许换行适应桌面宽度。分析场景使用普通下拉（10个业务选项），质量与权限提示复用notice。未进行浏览器渲染验证。
