# 外部数据源体验优化与检查记录

## U05 · 已确认范围（2026-09-10）

用户要求移除商户资讯预览、将两个切换入口优化为页签、清理新增和编辑来源的不当提示，并增加资讯内容的真实业务展示效果。沿用现有管理端路由和公共主题，不增加模块或采集功能。

## 已完成

- 删除外部数据源页的“商户资讯预览”入口和对应弹窗处理函数。单篇核验、人工发布、暂停展示和历史发布快照继续使用原流程，公开资讯投影及缓存写入保留。独立商户页仍加载其原有预置数据，不将其声称为管理端实时预览。
- 数据源、信息核验改为公共下划线页签，选中态与操作按钮区分，保持图标加文字。页签使用 tablist/tab/tabpanel、选中状态和焦点管理，支持方向键及 Home/End；各自保留已执行查询和页码。选择条件立即读取当前文字查询，文字仍由查询按钮或 Enter 提交。
- 新增和编辑表单删除顶部重复说明。来源地址明确为网站或栏目 HTTP(S) 地址，使用 URL 输入类型和 HTTPS 引导，独立字段名并关闭自动填充，错误定位仍对应来源地址。保存后分别提示新增待确认、无变化、修改待重新确认及受影响的信息/已发布资讯数量。
- 源码中未发现截图中的 `jdbc:nebula://地址:端口/库名` 提示；不能仅凭静态检查确定其浏览器或扩展来源。上述字段调整用于减少误匹配，本轮未宣称已验证浏览器侧提示消失。
- 9 个来源补充具体信息范围，保留原来源 ID、状态与地址。21 条资讯补充背景、统计范围、观察内容或明确的核验差异，正文分段；列表增加摘要与独立的发布状态列。详情保留原文折叠留档，主要展示稿标题随核验/发布状态变化。
- 正常供需及行业资讯的“平台同品类参照”取自 2026-08-17 至 2026-08-23 已复核采价和同周已映射市场数量，单位分别为元/吨和吨。引用事实初始化后冻结，不随后续采价编辑重写原文或旧快照。第35周鸡副周报原文时间调整至8月30日，晚于其观察期间。
- 问题条目分别说明安排到期、件数与净重及截止日冲突、缺少入库依据、库存截止日差异、来源缺失和期初错位。保留待核验及处理依据约束，不能只延长有效期或确认来源就自动发布。
- 通过既有构建脚本更新独立小程序的资讯及关联预置数据，仍为15篇资讯、5份行情、每户5份报告；不清理用户已有缓存或报告历史。所有内容和来源均为合成业务资料，没有访问真实机构或植入真实商户信息。

## 实现与检查

实现文件：`assets/js/admin-sources.js`、`assets/js/admin-news.js`、`assets/js/source-store.js`、`assets/css/common.css`，及既有 `assets/js/demo/` 预置内容。

新增 `node scripts/check-source-experience.mjs`，用实际来源控制器与 Node DOM 替身检查页签、键盘切换、筛选保留、来源限定清除、URL错误定位、不同保存反馈、引用数字与市场汇总一致性，以及来源修改后暂停发布、保留旧快照、重新核验仍需单独发布。

18项Node专项检查及通用静态检查通过，包含采价、映射、多品类、来源、资讯、分析、简报、报告、深化与二次完善、登录、系统管理、追溯、界面文案、商户移动结构及交互。全量检查首次执行时商户交互测试替身与同步调整中的商户页面结构不匹配；该脚本更新后单独复跑通过，未修改商户实现来绕过检查。通用检查覆盖98个文件、14条路由的语法、资源引用、UTF-8及基础CSS。过程结果在`work/category-regressions.json`，结合复跑与新增专项的最终记录在`work/source-experience-checks.json`。

仅进行了 Node 行为和静态代码检查，未启动服务或浏览器，未做布局、字号、截图或浏览器自动填充验证。工作区没有 Git 仓库，未提交或部署。
