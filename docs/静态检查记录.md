# 静态检查记录

## 系统管理入口整合（2026-09-10）

check-system.mjs已覆盖两个编辑入口一次保存资料和角色/权限、一次版本递增、状态只读与列表启停、无重复入口、新增默认启用，以及schema 1至2权限合并迁移。检查旧资料/角色关联/版本保留、迁移幂等、仅有旧分配权限的角色兼容、迁移写入失败保留旧缓存及未知权限不覆盖。仅Node与静态检查，未启动浏览器、服务或部署。

整合完成后，工作区全部16项Node回归通过（含已有界面文案、登录及商户端专项）；项目静态检查覆盖91个文件、13个路由，JS语法、本地路径、UTF-8、空白及CSS基础检查通过。当前无Git仓库，不适用git diff --check。

## U01系统管理与顶栏检查（2026-09-10）

系统管理新增check-system.mjs：用户/角色校验、唯一性、管理员保护、关联角色删除限制、多角色并集与停用生效边界、原样保存/过期版本、本地缓存校验/降级、当前人员同步与旧历史不变。Node事件替身覆盖查询触发时机、权限树搜索路径/空状态/查看依赖/父子勾选/半选、角色超过10条时搜索控件；HTML正文检查标签配对和异常输出。既有业务检查同步移除旧角色切换假设，改验固定管理员完整权限与退出门禁，审核发布及商户公开范围保持。

当前包含既有登录专项的13项Node检查及项目静态检查通过，9个管理端与4个商户端路由均有实现。未启动服务、浏览器、截图、Git或部署；Node替身和静态HTML检查不代表真实点击、布局或渲染字号验收。

日期：2026-09-09。范围：项目新建与基础框架。

- `scripts/check.mjs` 执行通过：普通 JS 脚本语法、HTML 本地资源引用、中文替换字符标记、行尾空白、CSS 大括号基础检查和禁用页面缩放检查。
- 管理端 7 个、商户端 4 个路由元数据检查通过，无同端重复 ID；这不是浏览器点击验证。
- `node --check scripts/check.mjs` 通过。
- PDF 与截图副本分别与原件 SHA-256 一致，详见 `materials/来源说明.md`。
- 新目录未初始化 Git，因此不适用 `git diff --check`；检查脚本已执行行尾空白检查。
- 未启动服务、未打开浏览器，未进行截图、字号渲染、布局或实际交互测试。没有后端权限实现或安全验证。

上述为框架建立时的历史检查记录。

## T01 每周平均采价列表与查询

日期：2026-09-09。

- 新增 `assets/js/admin-prices.js`，仅由管理端加载；公共导航仍由 `shell.js` 渲染，菜单配置未复制。
- `scripts/check.mjs` 执行通过：19 个项目文件、11 个路由定义；检查覆盖 JS 语法、HTML 本地资源路径、UTF-8 替换字符、行尾空白、CSS 大括号与禁止页面缩放。
- 源码核对：8 周 × 3 类共 24 条虚构记录；周、品类、状态下拉分别为 9、4、4 个选项（含全部），均未超过 10 条，无目录树；没有增加不可用的维护或导入按钮。
- 查询事件源码核对：文本仅 submit 执行，选择条件 change 执行并读取当前文本；重置恢复全部条件；查询后回到第一页；翻页复用当前结果；零结果清空表格并禁用翻页。每页条数改变重新查询并回到第一页。
- 布局源码核对：公共字号令牌复用，查询和分页按钮 36px，查询区允许换行，表格局部横向滚动；未使用页面缩放。
- 本项目无 Git 仓库，未执行 `git diff --check`，行尾空白由项目静态脚本检查。
- 未启动服务、未打开浏览器；未进行渲染字号对照、截图、真实点击、跨浏览器或布局视觉验证。上述源码核对不构成浏览器交互验收。

## T02 采价维护、CSV 导入、历史及复核

日期：2026-09-09。

- `scripts/check.mjs` 通过：23 个文件、11 个路由定义，覆盖脚本语法、本地资源路径、UTF-8 替换标记、行尾空白和基本 CSS 检查。
- `scripts/check-prices.mjs` 通过：有效日期与周一、正数及价格精度、单位、品类、备注长度、同周同品类重复；新增、复核通过、修改重回待复核、退回修改、无变化不新增版本。
- CSV 数据逻辑覆盖 BOM、CRLF、引号内逗号、换行、双引号转义、物理行号、错误表头、空文件内容、缺列、超过 500 条、文件内重复、已有记录冲突、未知品类和非法价格。
- 导入检查确认错误批次不写入；预览后新增冲突会在确认阶段被拦截；成功批次包含原始记录快照；后续编辑不修改批次快照和旧复核快照。
- 源码核对：真实文件读取仅在本地进行，UTF-8 解码失败和文件超过 2 MB 会提示错误；异步读取使用版本标记，关闭弹窗或重新选择文件不会复用过期结果。模板下载使用带 BOM 的 CSV；XLSX 未实现。
- 源码核对：取消弹窗不调用数据写入；记录变更后保留已执行筛选及合理页码；查询周被移空时仍明确显示该日期，不冒充“全部”。
- 仍无 Git 仓库，未执行 Git 操作。未启动服务、未打开浏览器；未进行文件选择器实际点击、模板实际下载、弹窗焦点、布局截图或字号渲染验证。Node 数据逻辑检查不等同浏览器交互验收。

## T03 品类映射

日期：2026-09-09。

- `scripts/check.mjs` 通过：26 个文件、11 个路由定义，覆盖 JS 语法、HTML 本地资源引用、UTF-8 替换标记、空白和基本 CSS 检查。
- `scripts/check-categories.mjs` 通过：映射与采价共用分析品类；12 个唯一来源 ID；已映射/未映射与品类/名称/编码组合查询；无结果场景。
- 数据逻辑检查覆盖无效目标、未知来源、超长说明、过期版本拒绝写入；相同内容不增加版本；多个原始名称归并；调整分组后数量更新，历史关系快照保留。
- 检查未映射项仅关联自身业务数据，且无价格误关联；库存数量符合期初＋入库－出库＝期末；映射调整不改写采价数据。
- 关联采价检查覆盖新增、复核通过和编辑后待复核，详情读取当前价格及版本，不使用过期副本。
- 源码核对：选择条件 change 立即查询并读取文本当前值；文本仅 submit 查询；翻页和保存使用已执行查询；取消不调用保存。分析品类下拉仅 3 个业务选项，无目录树。
- 页面复用公共菜单、图标、CSS 和 dialog；未改动采价实现。仍未初始化 Git，未执行 Git 操作；未启动服务或浏览器，未验证实际点击、渲染字号和视觉布局。

## T04 外部数据源与人工核验

日期：2026-09-09。

- `scripts/check.mjs` 通过：29 个文件、11 个路由定义，JS 语法、HTML 资源引用、UTF-8 替换标记、空白与基础 CSS 检查通过。
- `scripts/check-sources.mjs` 通过：共享品类、组合查询、来源必填/长度/类型/品类/地址校验、名称及规范化地址重复拦截、过期版本与重复提交保护。
- 数据逻辑覆盖来源待确认不能将信息核验通过，来源确认不自动确认信息，来源变更重新待核验，范围缩小后越界品类不可通过，异常重标记回到待核验，通过与不通过均留存意见。
- 检查原始内容、来源快照及旧核验记录不被后续编辑覆盖；新来源无自动信息；未通过或来源版本不匹配信息不可作为已核验依据。
- 源码核对查询选择型即时触发，文本仅 submit 查询；保存保留已执行查询；页面按钮复用公共图标和组件。来源与信息字段转义后输出，未实现网络采集或发布。
- 未启动服务或浏览器，未进行表单真实点击、弹窗焦点、视图切换、渲染字号和视觉布局验证；Node 数据逻辑检查不代表浏览器验收。未执行 Git 操作。

## T05 市场量价分析

日期：2026-09-09。

- `scripts/check.mjs` 通过：33个文件、11个路由定义，脚本语法、本地引用、UTF-8替换标记、空白和基础CSS检查通过。
- `scripts/check-analysis.mjs` 通过：96条周度样例期初＋入库－出库＝期末、相邻周连续、最新周与T03一致；入出库期间求和、库存只取末周、价格使用完整期间已复核采价均值。
- 覆盖1/4/8周范围、前一等长期间、上年ISO第53周缺失、缺数据/基期零/真实零值区分；未复核价格排除，全部品类不计算合成价格。
- 覆盖未映射排除、建立映射后汇总更新；采价复核通过、改价重回待复核和重新复核对结果的影响；价格5%演示阈值触发；来源发布时间与核验状态同时过滤。
- 源码核对图点与按钮关联相同周明细，缺失值不连线；SVG使用容器宽度计算并清理ResizeObserver；AI块包含待复核/未发布、期间、来源、生成时间及规则版本。
- 未启动服务或浏览器，未验证图表实际点击、SVG渲染、容器重绘、字号与视觉布局。未执行Git操作，数据逻辑检查不构成浏览器验收。

## T06 市场行情简报及商户市场行情

日期：2026-09-09。

- `scripts/check.mjs` 通过：40 个文件、11 个路由定义，脚本语法、本地资源引用、UTF-8 替换字符、行尾空白与基本 CSS 检查通过。
- `scripts/check-bulletins.mjs` 通过：周一/自然月/品类与必需数据校验，失败不新增记录；重复生成、空复核意见、未复核发布和已发布编辑拦截；退回修改、编辑清除旧通过结论、单独发布、修订草稿保留商户旧版和新版替换。
- 检查 API 返回副本、采价编辑、映射调整及来源核验失效均不改写旧快照；新生成读取当前数据。公开投影排除管理历史、原始映射、采价明细及内部意见，商户 HTML 未引用管理端数据脚本。
- 自然月计算覆盖跨周首尾片段、31 天总覆盖、周内均分总量守恒、月底库存与下周周末区分、价格按天数加权，以及闰年/平年二月边界。
- 本地保存/读取、公开缓存、存储禁用时本次内存降级、动态文案 HTML 转义与新 CSS 全部令牌有效性检查通过。
- 源码核对：生成关闭和路由切换取消计时，计时回调再次检查弹窗仍打开；选择条件读取当前文字即时查询，文字只 submit 查询；双端消息检查来源窗口，传递发布投影，关闭清理监听。
- 新样式复用全局令牌，图表固定 CSS 宽度保持轴标签基线，窄容器及明细表只在局部滚动。未启动服务、浏览器或执行 Git；尚未验证真实点击、iframe 跨文件消息、本地缓存浏览器差异、截图与渲染字号。上述检查不构成浏览器交互或视觉验收。

## T07 管理端商户经营报告与建议复核

日期：2026-09-10。

- `scripts/check.mjs`通过：44个文件、11个路由定义，脚本语法、本地路径、UTF-8替换标记、行尾空白及基本CSS检查通过。
- `scripts/check-reports.mjs`通过：三户八周的原始名称、品类及周汇总一致；期初＋入库－出库＝期末，相邻周库存连续，三户合计小于市场数量。
- 验证周期间累计和末周库存、周转算法与零分母、同期间市场数据一致；未复核价格不纳入均价，不形成价格方向建议；无效商户/周一/周数、数量不足及重复生成拒绝写入。
- 覆盖两项共同复核、空意见、退回、仅建议修改重回待复核、过期操作、已发布不可改、修订保留旧版及新版替换；发布接口限定商户并排除历史及内部意见。
- 验证映射/采价修改及返回副本不会改写旧报告；新生成读取当前数据；本地恢复及存储禁用降级；动态HTML文案转义和标签配对。
- 源码核对生成计时取消及存活检查、选择型即时查询与文字submit、商户HTML未加载新增管理数据；未向公共行情缓存写入本户报告。
- 复用已有CSS、导航和弹窗；未启动服务、浏览器或执行Git，未验证真实点击、弹窗焦点、渲染字号或视觉布局。

## T08 商户端经营报告与经营建议

日期：2026-09-10。

- `scripts/check.mjs`通过：47个文件、11个路由定义；JS语法、HTML资源引用、UTF-8替换标记、行尾空白和基本CSS检查通过。
- `scripts/check-merchant-reports.mjs`通过：商户页不依赖管理端全体商户数据；未发布、未知商户、其他商户或混合商户数组拒绝接收；本户缓存不读取管理端缓存。
- 验证同批仅留最新发布版，修订草稿不替换旧版；报告与建议分别记录阅读状态，阅读旧版不清除新版提示，本地恢复及存储禁用时内存行为符合设计。
- 验证白名单移除管理历史、内部复核意见及额外注入字段，坏数据不覆盖已有本户内容；同一发布快照在无管理端reportData时可渲染报告与建议，数值、版本、建议文字和参考声明一致，HTML标签及转义通过。
- `scripts/check-reports.mjs`回归通过，覆盖原有本户数据、共同复核发布、版本保护和快照，不因共享正文调整而改变数据行为。
- 源码核对父子窗口与商户ID校验、请求超时及路由清理；已打开旧版保持原快照，通过用户点击加载新版；同版互跳携带快照，未新增登录/账号切换/消息中心。
- 未启动服务、浏览器或Git；未验证iframe真实通信、不同文件缓存行为、实际点击、移动布局或渲染字号。静态检查不代表浏览器或安全验收。

## T09 商户行业资讯与审核后发布

日期：2026-09-10。用户确认资讯需要审核后发布。

- `scripts/check.mjs`通过：52个文件、11个路由定义；JS语法、HTML本地资源路径、UTF-8替换标记、行尾空白和基本CSS检查通过。
- `scripts/check-news.mjs`通过：未核验、来源未确认、过期版本及重复发布拦截；预置核验通过不自动发布，人工发布后方可进入商户数据。
- 验证异常标记、核验不通过和来源变更暂停展示；重新核验通过仍待发布，再次发布版本递增。历史发布快照、原文及原文时间保留，平台发布时间与核验时间分别记录。
- 验证发布投影白名单、坏数据不覆盖缓存、存储禁用降级、类型/品类/标题及来源组合查询、发布时间排序；商户不加载来源或管理端脚本即可渲染资讯，正文转义及HTML标签配对通过。
- `check-sources.mjs`、`check-analysis.mjs`、`check-bulletins.mjs`和`check-reports.mjs`回归通过，原有来源核验、量价依据筛选、简报和经营报告快照行为未受影响。
- 源码核对：查询选择型change立即读取当前文字，文本仅submit；返回保留条件及页码，接收移除内容时退出失效详情；嵌入页同步前不展示缓存，消息核对来源窗口，关闭/路由变化清理监听和计时。
- 沿用公共导航、按钮、表格、卡片、正文与dialog，无新CSS。未初始化Git，不适用git diff --check；已由静态脚本检查行尾空白。未启动服务或浏览器，未验证实际消息通信、跨文件缓存、真实点击、布局和渲染字号。

## T10 统一追溯、关联检查与收尾

日期：2026-09-10。

- `scripts/check.mjs`通过：55个项目文件、11个路由定义，检查JS语法、HTML本地资源、UTF-8替换标记、行尾空白、CSS括号及禁用页面缩放。
- `scripts/check-trace.mjs`通过：按双端HTML中的实际顺序执行全部业务脚本（不执行依赖DOM的shell渲染），确认管理端7个/商户端4个路由均注册，商户未加载追溯、来源及全体商户管理数据。
- 检查初始48条既有历史与预置标识，无映射历史时不补造日志，查询不写存储，返回副本不改写原始记录。覆盖操作日期边界/反向/无效日期、类型/模块/内容/商户关键词与无结果，事件ID唯一和时间排序。
- 检查新增采价导入与原文件/批次/行号关联、映射前后品类、资讯发布及来源变更重新核验；改名后的来源不覆盖旧核验/发布依据。
- 检查简报、报告编辑/复核/发布/修订均汇入追溯，旧版记录仍定位旧版详情，上游映射变化不改写生成依据。经营报告修订待发布时，商户保留V1；对应版本展示复用现有详情函数，报告与建议保持共同版本。
- 对生成的各类过程详情检查HTML标签配对、中文动态字段转义及undefined/NaN异常文案；关联详情复用入口按指定版本读取。属于Node与静态HTML检查，不是浏览器点击验证。
- 既有8项检查全部回归通过：check-prices、check-categories、check-sources、check-analysis、check-bulletins、check-reports、check-merchant-reports和check-news，覆盖数量、期间、单位、数据快照、核验门槛与商户发布范围。
- 源码核对查询事件、分页边界、日期错误、空状态、同一内容限定与清除、关闭弹窗和关联版本不可用提示。只新增追溯页面，未增加业务修改入口，未改变公共导航或CSS。
- README、任务清单、需求映射、设计规范、AGENTS和接续说明已更新，README补充演示顺序及刷新/缓存边界。未初始化Git，不适用git diff --check；未启动服务或浏览器，未做消息通信、实际点击、布局、截图或渲染字号验证。


## D00—D06深化检查（2026-09-10）

完成项目静态检查与10项Node回归：prices、categories、sources、analysis、bulletins、reports、merchant-reports、news、trace、deepening。新增专项从实际入口加载，覆盖140周、420条采价、1680条市场数量、三户非负数量/市场上界、同比与季节性、异常、单户基期过滤、改稿重审、汇总发布限制、角色与机构拒绝、跨户投影、质量场景阻止生成及旧依据不变。

修正了本户基期遗漏商户过滤和甲户末周负出库样例，并纳入回归。仍未进行浏览器通信、点击、截图、布局或渲染字号对照；不将Node/HTML检查写成浏览器验收。当前无Git仓库，未执行git diff --check；静态检查覆盖尾随空白、语法、路径、UTF-8及CSS结构。


## E01—E08二次完善（2026-09-10）

新增check-review-round.mjs，覆盖重新生成同期间V+1及旧版迁移、修正后的采价引用、失败不写入、原样保存不改变发布、问题原文与处理方式核验、季节平稳/跨年/并列高位、多关注项及结构基期、Q3原周明细与采用量、完整自然月基期、资讯方向核对、月度基期隐私门槛、展示状态与旧缓存识别。全部11项Node回归和项目静态检查通过。未进行浏览器、服务、截图、Git或部署。

## 追溯同内容限定入口移除（2026-09-11）

- 过程详情移除“同一内容记录”操作，列表移除内容限定提示及取消入口，查询层不再接受单独的`objectId`限定；保留内容/编号关键词查询和简报、经营报告的关联版本查看。
- `node scripts/check-trace.mjs`通过，新增入口源码断言，确认重复入口及限定分支已移除，原有追溯汇总、查询、依据、历史版本定位和转义检查继续通过。
- `node scripts/check-query-controls.mjs`与`node scripts/check.mjs`通过；后者检查124个文件和14个路由。当前无Git仓库，不适用`git diff --check`；未启动服务、未打开浏览器，未进行实际点击、布局或渲染验证。
