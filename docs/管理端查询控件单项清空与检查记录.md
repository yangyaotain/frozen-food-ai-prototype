# 管理端查询控件单项清空与检查记录

2026-09-10 · U08 · 用户已确认分析方案后实施。

## 范围及行为

- 覆盖每周平均采价、品类映射、行业资讯（资讯管理/来源管理）、市场量价分析、市场行情简报、商户经营报告、追溯记录、用户管理、角色管理查询区。
- 有具体值时，悬停或键盘聚焦显示框内“×”；空值/全部不显示，无悬停设备在有值时显示。
- 点击只清空当前条件，保留其他条件。下拉/日期发出一次change，沿用页面查询并回第一页；同时读取当前关键词。文字清空只发出input，仍由查询按钮或Enter提交。
- 普通下拉清回全部。采价周、分析品类、所属角色等可搜索下拉同时清空选值、内部搜索词、选中状态并关闭面板，焦点回到控件。
- 起始和结束日期各自清空，另一端保留。分析截至周/范围为必需条件，缺失时暂不展示旧结果并提示补选；分析品类清空后为全部品类。
- 整体重置保持现有规则；市场量价分析仍恢复2026-08-24、近4周、鸡副。
- 不修改编辑/生成/审核表单、分页条数、商户端、业务数据、审核发布规则或历史快照。

## 公共实现

- `assets/js/ui.js`：`searchSelect`返回可见元素并向原字段暴露选择器引用；`enhanceCategorySelects`保留原select作为表单值与事件来源；新增`enhanceQueryControls(form)`统一包装查询控件和绑定清空、焦点、输入/变更/reset同步。
- 管理端8个控制器覆盖9页显式接入；行业资讯重建页签表单后重新接入，不使用全局DOM观察或弹窗扫描。
- `assets/css/common.css`：复用字体、36px控件和颜色令牌，清空图标14px、点击区域24px；右侧预留下拉/日期入口空间，关闭已接入文字框的浏览器默认清除图标。
- `assets/js/admin-analysis.js`：分析范围增加空占位，缺截至周或范围时先清除分析结果，保留原数据校验、默认值及重置。

## 检查记录

- `check-query-controls.mjs`：用Node DOM/事件替身运行实际公共清空控制器，验证单项清空、文字延后查询、选择型单次事件、日期端点独立、搜索选择器同步与焦点、空值/禁用/只读、重复初始化、重置同步和管理端接入范围。
- `check-analysis.mjs`：验证清空分析范围无有效计算结果，以及原有期间、缺失/零值、采价和来源规则。
- 相关回归：`check-system`、`check-source-experience`、`check-source-collection`、`check-news-classification`、`check-trace`、`check-bulletins`、`check-reports`、`check-prices`、`check-categories`、`check-multicategory`均通过。来源相关两项测试更新公共UI替身入口，原业务断言保留。
- `check.mjs`：JS语法、HTML本地资源、UTF-8替换字符、行尾空白、基础CSS括号/缩放规则及路由检查通过。
- 当前目录不是Git仓库，未执行`git diff --check`；未初始化或提交Git。未启动服务、未打开浏览器，悬停/点击实际呈现、日期控件布局和渲染字号尚未做浏览器验证。
