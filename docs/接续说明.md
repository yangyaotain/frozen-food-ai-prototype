# 新会话接续说明

2026-09-14市场行情简报趋势图已按用户纠正口径完成双端同步：价格使用已复核周均价，库存使用周末或周片段期末库存，不拆分每日价格。`bulletin-view.js`对周报展示“本周平均价格/周末库存”单周指标，对月报按周片段绘制自适应趋势；管理端详情、编辑/复核参考栏、版本详情及小程序行情共用该组件，明细统一为周度量价明细。周均采价维护、复核发布、已保存版本、缓存及本户数量规则保留。检查入口为`check-bulletin-chart.mjs`，详见[趋势图双端优化记录](市场行情简报趋势图双端优化记录.md)。未浏览器验收。

2026-09-14市场量价分析改为“周分析 / 月分析”，默认周2026-08-24、月2026-07、鸡副；公共周搜索选择器回显完整日期，月控件选择年月，清空和切换立即查询、各页签独立保留与重置。月均价按已复核周均价在月内覆盖天数加权，月内明细按周片段展示，单周不绘制空旷单点趋势。新增`analysisStore.query({kind, date, category})`，旧周数接口仅供历史兼容。详见`市场量价分析周月维度优化记录.md`及`check-analysis-calendar.mjs`；未浏览器验收，本条替代下文T05旧截至周/近4周的当前页面说明。

2026-09-14市场行情、经营报告和经营建议双端深化已完成：`bulletin-store.js`新增2–4条行情重点，`report-store.js`新增2–4条经营结论、建议摘要和2–4条重点关注事项，旧缓存缺字段时只补结构化摘要，不覆盖原正文、状态和版本。管理端原页内工作台可编辑这些内容，报告与建议仍共同复核发布；小程序卡片和详情按摘要—重点—全文分层。默认已发布投影为5份行情（3周报、2月报）、9篇资讯，三户各4份报告及同版建议（2周报、2月报）。新增样例选择通过现有发布检查的历史期间，未把受限期间或当前待复核/需修改/待发布内容强行发布。详见`市场行情与经营内容双端深化记录.md`；仅静态/Node检查，未浏览器验收。

2026-09-14行业资讯双端内容深化已完成：`source-store.js`发布投影新增摘要、2–4条核心要点和关注事项，正文继续支持短标题分节；管理端列表、展示稿编辑、原文/展示稿核验和发布确认同步结构化内容。小程序首条使用既有强调卡片并标记“最新发布”，普通卡片显示摘要，最新卡片补充两条要点；详情按摘要、核心要点、资讯正文、关注事项及来源版本连续阅读，关键词同时搜索标题、摘要、要点和来源。默认已发布资讯为9篇（政策2、行业3、供需4），其余待发布、暂停及核验不通过状态保留；公共快照已整体重建，行情及三户报告数量和冻结内容未改。来源获取、登录、五项导航和其他模块不变。详见`行业资讯双端内容深化记录.md`；仅静态/Node检查，未浏览器验收。

2026-09-11小程序发布投影同步已完成：新增`merchant-demo-source.mjs`从管理端默认状态只读取真实已发布投影，`build-merchant-demo.mjs`整体重建公共及三户快照，不再自行发布额外内容、不再局部`--reports-only`重建或覆盖业务人员。当时独立小程序为3份行情、3篇资讯，资讯数量现由上方2026-09-14记录更新；丰茂/润泽/锦禾分别3/2/2份报告及同版建议。新版待复核时仍展示旧已发布版，其他管理状态不下发。新增`check-merchant-parity.mjs`逐字段防止名称、产地、采价、来源、正文、人员、版本和草稿隔离回退。页面结构、登录、导航、筛选、详情与本户隔离未改。详见`小程序与管理端发布投影同步记录.md`；本条替代旧独立内容数量及局部构建说明，仅静态/Node检查，未浏览器验收。

2026-09-11长内容详情统一优化已完成：`ui.js`的多层工作台为`openDetailPage`提供整页只读/表单承载，涉及各模块详情、历史版本、引用简报、追溯与批次，以及生成/获取记录。每层暂存上一层DOM并在返回时恢复，编辑页进入参考页不会丢失草稿；关闭/路由离开统一释放订阅。列表委托事件已排除工作台内操作。采价导入和角色编辑整页化，来源获取记录独立，发布资讯确认不含全文；CSS补充全宽及紧凑信息/前后对照，保留最新编辑左右布局。入口清单与检查边界见`长内容详情与记录页面统一优化.md`。此条替代旧文只读长详情、版本记录仍使用dialog的说明；商户端和业务存储/复核发布规则保持。

2026-09-11长内容编辑/复核工作台已完成：`ui.js:openTaskWorkspace`以页内二级工作区替代六个长任务弹窗，隐藏但不重建原列表，返回恢复现有页签、筛选、页码、滚动与焦点；history state支持浏览器返回，输入变化后离开有放弃确认，`enhanceTextareaCounters`提供字数提示。`admin-news.js`展示稿编辑、`admin-bulletins.js`编辑/复核、`admin-reports.js`编辑/共同复核、`admin-sources.js`待核验详情已接入；原文/展示稿对照、保存前报告或数据依据、固定操作栏及响应式单列使用公共样式。发布、短表单和非待核验只读详情仍用dialog，存储、版本、复核发布、商户端及其他页面未改。新增`check-edit-workspaces.mjs`并通过相关回归；未浏览器验证，详见`长内容编辑与复核工作台优化记录.md`。

2026-09-11状态样例已补齐：新增管理端`admin-example-states.js`在access包装前复用业务接口初始化状态，四类报告为4/4/8/8份，首屏覆盖4状态、按户保留周/月待复核与已发布；只推进未操作初始草稿，缓存及历史不重置。资讯增为26条、获取记录4个来源，生成历史9/7/6/7条含部分成功；角色增加停用历史资料查阅员，schema2通过exampleVersion一次迁移。原商户独立样例不变。以`各模块状态样例与检查记录.md`和`check-example-states.mjs`为最新状态覆盖依据；旧固定数据专项排除展示初始化，完整入口由新专项检查。

2026-09-11管理端周/月页签已拆分：`admin-bulletins.js`、`admin-reports.js`各有独立周/月查询草稿、已查询条件及页码，默认周报，列表按kind过滤；行情类型下拉移除。`admin-generation.js`共用tabs/selectTab与单类型顶部，bind通过当前类型回调处理调度刷新，设置/记录仍按按钮键定位。旧无kind周数报告保留周报列表及原期间标签；商户端/存储/调度未变。专项`check-generation-schedule.mjs`补充隔离、分页、草稿、重置、键盘和订阅刷新检查，详见`简报与商户报告定期生成及检查记录.md`最新节；旧同屏两行规则失效。

2026-09-10商户报告周/月统一已完成：`generation-schedule.js`采用week/month/report-week/report-month四计划，schema2兼容原键旧配置、日志和按原期间待补任务；`admin-generation.js`共用两行顶部、独立设置/记录和保存生效的开关。`report-store.js`新增自然月生成与月边界数量/周转/价格口径，旧报告快照保留，仅替换未操作初始四周草稿；管理端9份周/月样例。商户白名单、正文、周/月筛选同步，按户样例通过`build-merchant-demo.mjs --reports-only`重建，每户5份，公共资讯/行情未重写。检查新增`check-report-calendar.mjs`，配合调度、报告、小程序及追溯检查；详见`简报与商户报告定期生成及检查记录.md`最新节。未浏览器测试，旧近1/4/8周表单规则失效。

2026-09-10小程序持续登录已完成：改用localStorage键`frozen-mini-login-v2:<merchant目录URL>`，登录状态无自动到期，仅退出清除；首次登录后从Web入口的新标签、刷新、浏览器重开均复用。旧sessionStorage v1不再读取，本次更新后登录一次即可。shell移除到期计时，storage事件同步跨标签登录/退出，取消迟到请求，前台/历史恢复继续校验。手机号与验证码任意非空规则、默认商户、管理端会话及业务缓存不变。存储不可用时仍降级当前页面内存。已做Node检查，未浏览器验证。

2026-09-10最新小程序登录规则：手机号与验证码任意非空即可登录，不校验手机号格式/长度/关联范围。`merchant-auth.js:household`对三个预置号码保留原商户，其余输入使用demo-a，保存与恢复会话共用该映射。输入框移除11位限制，发送按钮也只检查非空；旧未关联商户提示已删除。号码掩码展示规则保留，未浏览器验证。

2026-09-10登录展示补充已完成（优先于下文）：`merchantUI.sheet`在showModal前设置top/right:auto，保持手机框底部定位；`merchantAuth.maskedPhone()`供登录主界面与授权框显示同一脱敏号码，默认139****0001，缺失时禁用一键登录，号码变化时不直接登录。短信验证码只要求非空，移除内容/位数/有效期/次数校验及发送前置条件；发送仅用于提示和倒计时。旧固定验证码规则失效，详见`小程序登录与退出设计及检查记录.md`。本轮仍不浏览器验收。

2026-09-10小程序登录/退出已完成：新增`merchant-auth.js`、`merchant-login.js`和`merchant-login.css`，由公共shell在未登录时展示独立登录状态；授权一键登录、验证码登录统一关联三户，`merchantProfile.currentId()`只读会话。“我的”退出经过确认，退出/过期/历史恢复均校验，清理页面与内存详情，保留按户阅读/发布缓存及管理端会话。原`?merchant`身份及免登录路线失效。体验号码、固定验证码、24小时会话、静态适配器边界见`小程序登录与退出设计及检查记录.md`；新增`node scripts/check-merchant-login.mjs`，原商户流检查改为显式登录身份。仅Node/静态验证，未浏览器验收。

2026-09-10部门完整路径回显与样式收敛：`common.js`新增只读`treePath(nodes,value)`，`systemStore.departmentPath`供列表及userBasics（管理详情/个人资料查看共用）展示。`ui.js:treeSelect`触发控件、ARIA标签、title、已选栏均显示完整路径；hidden input和保存值仍是原部门名称，未知旧名称原样显示、不补造上级。CSS取消选中叶节点的左侧inset蓝线，保留蓝字/浅蓝底/勾选，长路径允许换行，用户列表部门列适当增加宽度。浮层定位、搜索与角色功能不变。check-tree-select/check-system追加路径回显及保存值保留检查；无浏览器验证。

2026-09-10市场行情简报顶部最新要求：周报/月报各一行，横排执行周期、下次执行（分钟）、状态、数据范围及对应设置/记录。下次时间取定期计划和待补任务中较早者，执行中同步状态；停用保留周期，下次栏及状态显示已停用。数据范围沿用最近记录，详细结果保留悬停和记录；月报入口直达月报页签。`generation-history.js`保留8条周报、6条月报，实际同类型同时间优先；旧缓存、队列和报告不重置，商户报告不变。本条替代此前分层展示及最近执行时间列，详见`简报与商户报告定期生成及检查记录.md`。

2026-09-10部门树扩充与浮层美化：公共目录`systemStore.departments`扩为6个中心/24个部门，保留原有ID/部门名称，旧资料不重置。公司/中心/部门使用`common.js`公共SVG图标；`ui.js:treeSelect`以按钮触发原生Popover顶层面板，fixed定位与`treeSelectPlacement`负责上下翻转和视口边界，浮层列表独立滚动，不撑开表单/弹窗。选中路径默认展开，其余中心默认收起；搜索保留并展开命中父路径；点击外部/Escape/选择/清空关闭，弹窗关闭或宿主移除清理事件与观察器。支持窗口缩放/弹窗滚动定位跟随，原选择/保存/旧值兼容不变。检查新增`node scripts/check-tree-select.mjs`，同时检查system与query-controls；未进行浏览器视觉验收。本条替代此前3部门示例及表单内展开说明。

2026-09-10追加市场行情简报配置布局优化：顶部完整自动生成卡片改为右对齐的周报设置、月报设置、生成记录三个入口。周/月报分别弹窗、分别保存，分析品类默认折叠；设置中不含执行情况或记录。生成记录使用独立宽弹窗和周报/月报页签，显示对应当前状态及历史。仅调整简报页面组织，商户报告页面与底层调度、补生成、复核发布规则不变；`check-generation-schedule.mjs`已同步验证新结构。

2026-09-10追加用户资料优化：`admin-system.js`用户列表账号/姓名分列，增加邮箱/部门；用户详情不显示更新时间/资料版本。所属角色查询用原生select并接入单项清空，编辑直接角色复选，均取消搜索；角色权限搜索不变。`systemUI.profile`为右上角独立资料入口，仅编辑姓名/手机号/邮箱/部门，通过`systemStore.saveProfile`字段白名单保存当前用户，刷新顶栏并以`system-user-updated`事件刷新当前用户列表，保留已执行筛选。版本继续用于旧弹窗覆盖检查。`system-store.js`保持schema 2并补齐缺失字段，仅替换匹配原始ID/账号/职务姓名的预置姓名，自定义资料和角色关联保留；新字段选填并校验格式/长度。详见`系统管理与公共顶栏设计.md`最新补充，旧文可搜索角色和右上角复用管理详情的描述由本条替代。

2026-09-10追加简报与商户报告定期生成：两页移除手动生成、重新生成及管理端商户预览，用公共“生成设置”配置并查看最近/下次执行。共用`generation-schedule.js`和`admin-generation.js`，周/月报分别启用，商户报告每周或每月按户生成、保留近1/4/8完整周；按期自动生成待复核，人工发布。失败队列每5分钟按原期间和原配置补生成，成功稿件及历史版本不覆盖；配置/队列保存在单独计划缓存，报告缓存原样保留。只在有效管理端页面存续期间调度；独立小程序入口与预置内容不变。专项`node scripts/check-generation-schedule.mjs`通过。详见`简报与商户报告定期生成及检查记录.md`；旧文手动生成和嵌入预览路线不再适用，旧生成API仅开发兼容用途。

2026-09-10追加U08：管理端查询区统一框内“×”单项清空，覆盖9页和行业资讯双页签。公共`ui.js`提供`enhanceQueryControls(form)`，在查询控件/搜索选择器创建后调用，页签重建时重新接入；不在公共弹窗或商户端自动执行。原生字段仍是表单取值与change事件来源，清空搜索选择器时同时重置面板词和选中状态；文字清空只更新输入，不自动查询。分析截至周/范围缺失时清除旧结果并提示补选，品类清空查询全部，默认和重置仍为2026-08-24/近4周/鸡副。专项`node scripts/check-query-controls.mjs`与相关回归均通过；仅Node事件替身/数据及静态检查，未浏览器验收。详见`管理端查询控件单项清空与检查记录.md`。

2026-09-10追加U07：行业资讯来源管理已补充“获取设置”和“立即获取”，按来源维护自动开关、栏目/接口规则及每日两次/每2小时/每日一次周期；列表显示最近结果和下次时间，弹窗保留获取记录。新增进入待核验，原文更新重审并保留旧快照，核验通过后仍单独人工发布。首次近30天，后续带重叠窗口去重；自动失败5/15分钟最多重试2次，持续失败待人工重试。仅周期修改不改变业务审核状态。新增`source-collection.js`和`admin-source-collection.js`，管理端页面存续期间本地调度虚构文章，未接真实采集或后台持久化，独立小程序入口不变；详见`行业资讯获取机制与检查记录.md`。检查新增`node scripts/check-source-collection.mjs`，未浏览器验收。

2026-09-10市场量价分析术语调整：对比表标题改为“本期、上期与去年同期对比”，列名使用“本期／上期／去年同期”。统计口径分别解释环比与同比，缺失/零值提示按对应期间显示；周环比异常描述使用“上周”。本轮仅调整显示文案，统计计算和历史快照保持不变。

2026-09-10补充默认展示要求：市场量价分析首次进入和重置统一使用截至2026-08-24、近4周、鸡副，完整展示指标、同比环比、2024/2025季节对照和3条已核验资讯。数据完整时显示正向说明；保留手动查询缺失数据时的真实提示，不改写上游审核或已生成快照。本次仅Node及静态验证，未浏览器验收。

2026-09-10用户确认：市场量价分析已移除“分析场景”下拉及说明；页面查询与重置均不再调用 `setScenario`。仅保留截至周、分析范围、分析品类及重置，选择即查询，质量与发布检查自动展示。`analysisStore.setScenario` 仅留给开发检查使用，旧文中的场景下拉验收路线不再适用。既有简报、报告快照及审核发布规则保持不变；本次仅静态检查，未浏览器验收。

2026-09-10追加U06：管理端资讯模块已按确认改名“行业资讯”，默认资讯管理、另有来源管理。文章自身保存分类，来源type仅作默认分类，双端共用newsTypes；分类改变暂停原发布并重新核验，核验通过后仍须人工发布，原文和历史分类保留。分类/发布状态可组合即时筛选，发布弹窗明确小程序栏目及分类。见`行业资讯分类与双端对应优化.md`。原sources路由、独立小程序预置入口保留，未恢复资讯预览；本轮不处理其他模块的数据联动。

2026-09-10管理端标题模块已按确认移除：`shell.js`不再为9个管理路由生成内容大标题和说明；`common.css`清理对应标题块样式，业务内容自然上移。保留导航、顶部路径及用户区和内部区块标题；菜单切换聚焦具名主内容区，`ui.js`同步弹窗备用焦点。商户页面及业务数据不变。本轮仅静态及Node检查，未浏览器验收。

2026-09-10 U05外部数据源完成：已删除商户资讯预览入口及弹窗函数，数据源/信息核验改为公共下划线页签并保留各自查询；来源表单使用网站地址引导及准确保存反馈。9个来源和21条资讯已充实，正常资讯平台参照来自8月17日至23日初始化采价及市场数量，原文和旧快照冻结，核验/发布状态分列。独立小程序内容由既有脚本重建，未清理缓存。详见[外部数据源体验优化与检查记录](外部数据源体验优化与检查记录.md)。旧文中商户资讯预览说明不再适用；本轮仅Node及静态检查。

2026-09-10 U04完成：小程序去除一级顶栏、重复标题、“我的”重复入口及清除筛选/刷新/加载更多按钮。共享`merchant-scroll.js`处理触摸下拉、桌面鼠标拖动、方向与距离判定、取消和滚动加载；每次追加4条，不重建已显示卡片。`merchant-app.js`以同路由history条目组织详情、同版报告/建议和引用简报，返回恢复筛选、数量、位置与已展开内容；新版本不强行替换正在阅读的旧快照，撤下内容返回列表，嵌入预览不回退样例。初次骨架、更新和加载状态均已接入。详情见`小程序体验优化与检查记录.md`U04；仅Node/静态验证，未浏览器验收。U03多品类、登录和系统管理的现有改动保留。

2026-09-10 U03完成：按用户确认把三类扩充为12类，双端共用`category-catalog.js`，数据为140周/1680条采价/39条原始名称/5460条市场数量，三户分别经营5/2/7类。多品类筛选、分析汇总、来源、简报、报告和小程序已贯通；独立商户内容为15篇资讯及5份行情、每户5份报告。缓存、历史快照及审核发布规则保持，新增数据不会改写旧三类内容。继续从既有路由改动，详见[多品类扩充与检查记录](多品类扩充与检查记录.md)。未浏览器或视觉验证。

2026-09-10 U02完成：小程序移除右上角返回登录页，增加整体边框/圆角/阴影，采用框内内容滚动；新增“我的”路由`profile`，共5项公共底部导航。商户资料及名称统一在`merchant-profile.js`，仅展示本户身份、编号、所属市场、经营品类和现有报告/建议入口。滚动恢复操作必须使用`.merchant-content`；弹层定位跟随外框并清理尺寸监听。详见`小程序体验优化与检查记录.md`最新补充。仍仅Node与静态检查，未浏览器验收。

系统管理后续整合已完成：用户角色分配、角色权限配置分别并入各自新增/编辑弹窗；两个列表只保留详情、编辑、启用/停用、删除。状态在编辑中只读，新增默认启用。系统管理两页的assign权限并入maintain，原本地键不变，schema 1兼容迁移至2，保留用户关联与资料版本。规则及专项检查见`系统管理与公共顶栏设计.md`，不要恢复独立分配/配置入口。

2026-09-10 商户小程序体验优化已完成：四个既有模块共用`merchant-ui.js`、`merchant-view.js`、`merchant-app.js`和`merchant.css`，不再直接套用管理端长报表呈现。独立入口加载6篇资讯、5份行情和按本户加载的5份报告/建议；管理端嵌入预览不加载或回退到预置数据。详情、存储边界、构建方式与验收路线见`小程序体验优化与检查记录.md`。新增`check-merchant-mobile.mjs`和`check-merchant-flow.mjs`，后者仅Node DOM替身，不是浏览器。沿用当前工作区登录入口和`presentation.js`显示名称更新；没有修改真实业务数据或扩展商户菜单。

2026-09-10 界面文案要求更新：用户明确要求高保真业务界面，移除双端所有“演示数据/静态原型/虚构样例/规则模拟”及本地存储、刷新恢复等说明。登录页账号密码仍默认填充，小程序入口仍新标签打开；AI标识、业务期间、单位、统计口径、审核发布状态及经营参考提示保留。商户、来源、操作人员使用业务名称，生成内容与独立小程序数据同步更新。`presentation.js` 仅规范旧缓存的显示文案，不改写缓存、数值或版本；旧P2规则标识继续兼容。新增 `scripts/check-interface-copy.mjs` 检查双端内容、旧快照只读、发布兼容与转义。后续页面按此要求执行，旧文档中要求在界面标注演示/虚构的内容不再适用。技术边界仍保留在开发文档中，未进行浏览器或视觉验收。

2026-09-10 U01完成：公共顶栏取消机构/角色切换与重复提示；固定管理员全功能展示，新增用户管理、角色管理（含搜索权限树），右上角用户信息与退出。管理端9个、商户端4个路由；13项Node回归（含登录专项）及静态检查通过。设计、保护规则、存储边界与实现入口以`系统管理与公共顶栏设计.md`为准。用户角色为前端配置展示，现有登录仍仅内置管理员；旧D版角色切换说明已被替代。

2026-09-10 登录入口调整：根目录 `index.html` 已改为蓝白主题登录页，账号 `admin` / 密码 `123456`，成功进入管理端“每周平均采价”。仅使用内置管理员演示身份，不增加其他账号登录或密码管理。小程序端保留一条直接链接。`login.css` / `login.js` 负责入口样式及表单，`auth.js` 负责管理端演示登录状态，与既有 `account.js` 用户菜单、`access.js` 会话门禁衔接。退出返回登录页，不清理业务缓存；直接访问管理端及退出后返回均重新校验。移除旧 `?session=start` 直接启用会话逻辑。文件直开通过同标签页 `window.name` 传递项目范围内的演示身份，管理页写入 `sessionStorage`；存储不可用时保留窗口身份，不传递密码。上述仅是静态演示，不是真实认证。新增 `node scripts/check-login.mjs` 验证表单状态与演示会话，未浏览器或渲染验收。

当前补充（2026-09-10）：E01—E08二次完善已完成，详见 `二次完善任务与检查记录.md`。同期间重新生成、资讯处理/原样保存、季节边界、多关注项、原周数量留档、展示状态和完整月度对照已接入；11项Node回归与静态检查通过，未浏览器验收。旧文中“改用其他期间生成”和Q2说明由本轮规则更新。

## 当前版本：D00—D06深化完成（2026-09-10）

用户确认后7包全部实施，保持11个路由与公共主题。当前数据已升级为140周、420条采价、1680条市场数量，三户采用不同经营轨迹；同比、季节对照、数量异常、混合供需、本户基期和组合建议已细化。资讯原文与展示稿分离，整理后重审；共用角色/机构入口、整期汇总限制、来源原文地址和质量批次已接入。完整规则和16条复验以 `docs/深化功能设计与逐项复验.md` 为准。

新增 `check-deepening.mjs`，共10项Node回归及静态检查通过。旧缓存不删；缺P2发布检查的旧版本仅管理端留档。新快照含safety/quality及引用ID/地址，公共投影避免携带内部质量/原文处理资料。质量场景可从分析页切换并影响后续生成，重置恢复。机构角色属前端演示，不是真实安全控制。

下面T01—T10为历史实施记录，其中24条采价、96条数量、8周与固定份额等记载是旧版本，不作为当前数据说明。原需求审计文件也保留为深化前证据。未启动服务、浏览器、Git或部署。

## 已完成

2026-09-09 根据需求 PDF 和需求表截图新建项目。已归档原件，建立双端入口、公共导航、样式令牌、页面注册机制和静态检查脚本。没有原项目代码被修改。

2026-09-09 用户已确认管理端 7 个模块、商户端 4 个模块的整体设计方案，沿用现有蓝白主题，先完成每周平均采价作为样板。执行清单见 `docs/设计任务清单.md`，具体未定业务口径见清单，不将演示建议当成正式业务规则。

T01 已实施：管理端 `prices` 路由通过 `assets/js/admin-prices.js` 注册列表，包含采价周、分析品类、复核状态和品类关键词组合查询、重置、10/20/50 条分页及无结果状态。共 24 条虚构样例，覆盖 2026-07-13 至 2026-09-06 的 8 周与鸡副、水产、猪副；页面注明周一至周日及人民币元/吨为演示口径。公共 CSS 增加查询控件、表格、分页、空状态和状态标签。文本输入不自动查询，选择条件变化读取当前文字，普通翻页使用已执行查询结果。切换路由或重新打开页面会恢复初始查询；本阶段没有持久化需求和实现。

T02 已实施：`price-store.js` 管理统一采价数据、CSV 解析、字段校验、复核状态、不可变操作快照与导入批次；`price-actions.js` 实现新增编辑、导入、详情和复核弹窗；`ui.js` 提供公共原生 dialog 与搜索选择器。当前管理端按 common、pages、ui、price-store、price-actions、admin-prices、shell 的依赖顺序加载。

导入采用 UTF-8 CSV，最多 500 条、2 MB，5 列依次为采价周起始日期、分析品类、周均价格、单位、备注；实际读取本地文件、下载带虚构样例的模板，不发送网络请求。有错误整批不写入，确认时重新校验重复，成功批次保存导入时快照。新增和修改进入待复核，复核意见必填；已复核内容改变后新增数据版本并重回待复核；无变化保存不新增版本。预置记录的初始状态明确标为样例，不补造真实审核过程。

T02 替代 T01 的状态行为：采价数据与操作记录在当前打开页面内保留，切换菜单仍保留，刷新恢复初始样例；查询条件在重新进入路由时恢复默认。保存后使用已执行查询刷新列表、保留当前分页范围，不将尚未提交的文字自动用于查询。采价周改为可搜索下拉，支持动态新增周次及无匹配提示。

T03 已实施：`category-store.js` 管理 12 条虚构原始名称及其与分析品类的映射，初始 9 条已映射、3 条未映射；`admin-categories.js` 注册管理端 `categories` 路由，提供筛选查询、分页、建立/调整关系和关联详情。两个脚本在采价模块后、shell 前加载，分析品类直接复用 `priceData.categories`，没有新增独立目录或修改采价逻辑。

原始名称及来源编码只读，一条来源只有一个当前映射；编辑校验目标品类、200 字说明及过期版本，保存不会重复插入来源记录，无变化保存不增加版本。多个原始名称可归入同一品类；调整后关联数量按新分组展示。维护动作在数据中保留前后关系，未新增独立日志页面。

关联详情展示同品类原始名称、国产/进口演示产地、2026-08-31 至 2026-09-06 的入库量、出库量及期末库存（吨），并直接读取当前采价记录及复核状态/版本（元/吨）。业务样例数量遵循期初＋入库－出库＝期末；未映射项仅展示自身业务样例、不关联任何品类价格。原始名称不是完整业务标准，所有数量均为虚构。映射状态在当前打开期间保留，刷新恢复样例，查询条件在重进路由时恢复默认。

T04 已实施：`source-store.js` 管理来源与信息状态、校验及快照，`admin-sources.js` 注册 `admin:sources`。初始 6 个来源（4 已确认、2 待确认）、12 条关联信息（4 预置核验通过、8 待核验，其中 6 条有异常标识）。来源、地址和内容均为虚构；地址使用 example.com 子域，仅文本展示，不访问、不采集。新增来源没有关联信息，不自动伪造采集结果。

来源资料包括名称、类型、HTTP(S) 地址、适用品类和信息范围；编辑后待确认，关联信息重新待核验。源资料版本、原文来源快照、异常标识和历史核验意见保留。来源确认与信息核验分别操作，意见必填；核验通过要求来源确认、版本一致和品类范围匹配，不通过结果也保留。详情可重新标记异常转人工核验。`sourceStore.eligible(item)` 提供后续分析可引用条件，不代表发布。

数据源与信息核验使用同一菜单下两个视图，分别保留已执行查询；查看来源关联信息时限定来源，可清除来源限制。查询型选择立即查询、文字按钮或 Enter 查询；新增编辑保存保留当前结果筛选。所有状态在本次打开期间保留，刷新恢复样例。T04阶段尚未定义资讯发布；T09已由用户确认逐篇核验后单独发布，详见下文。

T05 已实施：`analysis-store.js` 汇总当前采价、品类映射与来源状态；`admin-analysis.js` 注册 `admin:analysis`，`analysis.css` 提供复用令牌的局部指标和SVG图表样式。默认截至2026-08-31、近4周、猪副；可切换本周/4周/8周与全部品类。条件选择或清除立即计算，日期无效或非周一清空结果并提示。

数量数据为12个原始名称×8周，共96条虚构样例，覆盖2026-07-13至2026-09-06；最新周完全取T03数量，向前生成并满足相邻周库存衔接。按当前映射统一汇总所有期间，未映射名称排除。期间入出库累加，库存取末周期末值。单品类价格使用已复核周均价的期间等权平均，任一周不足不计算期间均值；全部品类价格分别绘图，不跨品类平均。

环比使用前一等长周期间，同比使用上年相同ISO周及周数；上年无第53周、缺基期或基期零均不硬算。价格未复核或缺失时图表断开。点击或键盘激活图点、明细按钮可查看对应周原始名称、品类、产地、期初/出入库/期末与采价复核版本。图表按实际容器宽度计算坐标，轴文字沿用12px令牌，不随整图放大；容器变宽时重绘，路由切换清理观察器。

AI分析参考仅为规则演示：价格周环比绝对值≥5%触发关注提示；库存净流入仅解释流量，不推断真实供需；季节性显示8周样例不足。包含AI标识、期间、生成时间、规则V1、待复核/未发布及无发布版本信息。引用来源同时按信息发布时间、品类与`sourceStore.eligible`筛选；无匹配信息则明确为空。后续简报可复用`analysisStore.query`，不要把未复核内容作为已发布内容。

T06 已实施：`bulletin-store.js` 负责简报数据快照、文字变更、复核发布、修订与版本过程；`admin-bulletins.js` 注册 `admin:bulletins`。列表支持日期交集、类型、状态、标题与 8 条分页，选择型即时查询、文字按钮或 Enter 查询。生成延迟 700ms 模拟处理，关闭或路由切换会取消计时；数据不足与重复期间/范围失败不写入，支持调整条件重试。默认生成 2026-08-17 周报，预置 2026-08-24 全品类周报已完成明确标注的模拟复核发布。

生成按品类分别读取当前已复核周均价、已映射市场数量和本期核验信息。周报为周一至周日；月报为自然月，数量按 0.01 吨精度周内均分到日、余数分配至周日，保持周合计，月末库存按当日累计流量计算；价格按周均价覆盖天数加权估算，不是实际逐日报价。月趋势按月内周片段显示，首尾片段天数标出。2026-08 猪副可直接生成月报，鸡副/水产需先完成 08-31 周的采价复核；日期超出数量样例范围将提示不足。

简报状态为 pending 待复核、revision 需修改、approved 待发布、published 已发布。复核意见必填，编辑变更后清除当前通过结论并重回待复核；没有实际变化的保存不改变状态。退回需修改文字后重新送审。发布仅允许最新通过复核版本，已发布不可编辑；修订克隆原数据快照，生成 V+1，重新通过并发布前商户继续看到旧版。原始生成时间保留，修订时间记录在该版历史。价格、映射和来源改变只影响之后生成的新简报，不改写既有快照；管理端详情可展开引用采价及映射版本，文字编辑记录保留前后文本。

`market-data.js` 定义已发布汇总投影与缓存；公开内容不含管理端历史、原始业务行、映射/采价明细、复核意见。`bulletin-view.js` 提供双端统一正文、分品类量价指标、价格/库存图和完整明细；`bulletins.css` 复用全局令牌。`merchant-market.js` 注册 `merchant:market`，支持月份/品类/类型筛选、已发布列表/详情/刷新；其他商户路由已在T08、T09完成，详见下文。商户 HTML 不加载管理端数据模块。

状态保存：管理端简报/版本及历史使用 `frozen-admin-bulletins-v1` 本地缓存；公开投影使用 `frozen-market-published-v1`。上游 T01—T05 仍刷新恢复样例，所以历史快照不应与刷新后的输入强行覆盖同步。浏览器禁止本地保存时降级本次内存演示。直接打开两个 HTML 时缓存未必共享，应从管理端“商户端预览”进入嵌入页，通过 postMessage 请求/响应同步公开汇总，接收方检查消息来源窗口，关闭清理监听。独立商户页可读自身缓存并监听同源更新；不是跨文件/多用户真实同步或安全控制。真实跨文件交互仍未浏览器验证。

T07 已于2026-09-10完成。新增 `report-store.js`、`report-view.js`、`admin-reports.js`，只由管理端加载，注册 `admin:reports`；复用公共导航、查询、表格、dialog及简报正文CSS。列表支持商户/日期交集/状态/标题和8条分页，选择型即时读取文字，文字按钮/Enter查询；保存沿用已执行条件。

业务样例为 demo-a/demo-b/demo-c 三户。按原始名称组从市场周数量分配不同固定演示份额，期初分配后逐周按入出库计算期末，保持库存连续；其余市场份额不展示为其他单户明细。本户及市场统一使用当前品类映射，未映射排除。预置截至2026-08-24近4周的3份待复核报告，默认生成截至2026-08-31近4周，支持完整1/4/8周。缺本户数量则不生成；市场缺已复核价格仍生成数量分析，但该品类不形成价格方向建议。生成关闭和路由切换取消700ms模拟计时。

周转采用数量口径：平均库存＝（期初＋期末）÷2，期间次数＝出库量÷平均库存，天数＝期间天数×平均库存÷出库量。零平均库存不算周转，无出库不算天数；28天为演示关注阈值，不是财务成本周转或实际批次库龄。详情有四项数量、周转、库存品类占比、周度及原始名称/产地明细、同期间市场价格/数量/环比、期间内已发布简报快照、采价版本及核验来源。建议含本户、期间、量化依据、固定边界及参考声明。

报告与建议共用 version；内部 revision 每次操作递增，用于拒绝旧弹窗覆盖。edit/review/publish/revise 均要求 id、version、revision；review 还要求结论、意见与 report/advice 两项确认。仅改建议也清除原通过结论；复核通过后单独发布，已发布不可编辑，修订继承数据与建议快照，V+1发布前保留旧版。历史保存生成、编辑前后、复核、发布和修订；生成时间不随修订重写。

管理端状态缓存为 `frozen-admin-reports-v1`，禁用存储时仅本次内存；不向公共行情缓存写本户报告。`reportStore.publishedFor(merchantId)` 只返回指定已知商户各报告最新已发布版，不返回管理历史、内部复核意见及revision。T08不得在商户HTML直接加载含全部商户数据的report-store，应传递选定商户的发布投影。T08已解除商户发布正文对reportData.states的依赖，未引入全部管理数据。演示隔离不代表真实安全权限。

T07检查：`scripts/check.mjs`和`scripts/check-reports.mjs`通过，覆盖本户汇总和库存连续、市场总量边界、周转分母、缺失价格、共同复核、过期操作、修订和本户发布范围、快照及本地保存、HTML配对/转义。未进行浏览器点击或渲染字号验证。

T08 已于2026-09-10完成。新增 `report-client.js` 和 `merchant-reports.js`，注册既有 `merchant:reports`、`merchant:advice`。商户加载公共ui、bulletin-view和report-view，但不加载report-store、analysis-store或其他管理数据。report-view增加advice呈现模式，已发布状态可独立渲染，不依赖管理端reportData。报告和建议共用同一快照，建议模式突出建议、市场参照、依据、边界与来源。

管理端经营报告行内新增“本户预览”，iframe地址使用 `merchant/index.html?merchant=demo-a#reports` 等已知演示商户ID；默认直接进入商户页为demo-a，未知ID展示提示，不回退到其他商户。商户页不提供商户切换控件。父页仅响应来自该iframe、且商户ID一致的 `frozen-reports-request`，发送 `reportStore.publishedFor(merchantId)`；商户接收 `frozen-reports-published` 时校验父窗口、商户ID、发布状态和结构。请求2500ms无响应提示重新从本户预览进入，路由切换清理监听和计时。

`reportClient.createStore(merchantId, storage)`接受发布内容并做字段白名单投影，拒绝草稿、其他商户及混合商户数组；同批多个版本只留最新。缓存键为 `frozen-merchant-reports-v1:<merchantId>`，阅读记录为同键加 `:seen`，只读本户缓存，不读取管理端报告缓存。阅读按reports/advice、报告ID、已读版本区分；阅读旧版不能消除新版提示。禁用存储时本次内存仍保留，刷新可能丢失。此为静态演示范围控制，不是安全权限或真实多用户同步。

两页支持月份交集及1/4/8周筛选、重置、列表/详情、空状态和刷新。列表展示未读/有更新，打开详情后记录该类型已读版本。收到新版时保留当前阅读的旧快照，点击“查看最新版本”后才切换并标读。报告与建议互跳通过 `app.reportNavigation` 携带当前快照及目标路由，保持同版；引用市场简报用公共dialog展示原快照。更新提示仅在本模块，不新增消息中心。商户端不自动发布预置的待复核报告；需先在管理端共同复核发布，再进入本户预览。

T08检查：`scripts/check-merchant-reports.mjs`、`scripts/check-reports.mjs`和`scripts/check.mjs`通过，覆盖本户缓存、未发布/混合商户拒绝、修订发布与阅读版本、禁用存储、白名单投影、无管理数据脚本时共用正文及HTML标签/转义。未启动浏览器，消息收发、点击和渲染字号未实际验证。

T09已于2026-09-10完成，用户明确“确认，需要审核后发布”。复用T04的信息核验作为逐篇审核，核验通过后需单独人工发布；预置核验通过样例也不自动发布。`source-store.js`增加publish/newsPublished，发布核对信息及来源当前版本；重复发布拒绝，来源变更或标记异常清空当前publication，保留releaseVersion和历史发布快照。重新核验后仍待发布，下次发布递增发布版本。此规则已确认，不再重复询问。

`admin-news.js`提供发布弹窗和商户资讯预览，接在既有外部数据源的信息核验视图中，不新增导航。`news-data.js`白名单投影只含公开正文、类型、品类、来源、原文时间、核验及发布元信息，缓存键为frozen-published-news-v1。来源/资讯仍保持T04的本次内存行为；管理端刷新恢复未发布样例并更新公开缓存。文件直开不同页面缓存未必共享，应从管理端“商户资讯预览”进入；窗口请求frozen-news-request，响应frozen-news-published，核对来源窗口，只传已发布资讯。存储不可用时仍可通过预览同步本次发布投影，不代表真实权限或跨用户同步。

`merchant-news.js`注册既有merchant:news，`news-view.js`渲染卡片与详情，复用原有移动卡片、查询和简报正文样式，无新CSS。类型及品类选择立即查询并读取当前文字，文字仅查询按钮/Enter提交；6条分页，返回保持筛选页码。接到失效资讯更新时退出该详情，不继续展示已暂停内容；嵌入页同步前不展示旧缓存。详情区分原文发布时间与平台发布时间，显示核验人/时间、来源/信息/发布版本。所有内容均虚构，未进行AI改写。

T09检查：check-news、check-sources、check-analysis、check-bulletins、check-reports及check.mjs通过，覆盖发布门槛、异常与来源失效、重新核验不自动发布、版本及历史快照、公开缓存、组合查询、正文转义及HTML标签。未进行浏览器消息收发、点击或渲染字号验证。

T10已于2026-09-10完成。`trace-store.js`汇总priceStore.records、categoryStore.records、sourceStore.sources/items以及bulletinStore/reportStore全部versions中的history；只读返回副本，不新增持久化或补造记录。初始48条历史均属预置样例；预置映射没有history，因此不人为生成对应记录。后续真实演示操作动态汇总，按时间倒序、同内容版本与操作顺序排序，稳定ID由模块/内容/版本/事件序号组合。

`admin-trace.js`注册既有admin:trace，操作日期/模块/类型/关键词查询与10条分页；关键词匹配内容、编号、商户及操作人。日期反向或无效显示错误，选择型立即使用当前文字查询，文本仅按钮/Enter，刷新沿用已执行条件。详情展示各次操作意见及前后留存、业务期间、来源和版本依据，不把操作日期当作数据期间；过程详情保留“查看关联版本”，不再提供重复的同内容限定入口。

采价导入依据关联批次、原文件和行号；来源核验和资讯发布按历史来源版本引用，后续改名不改写旧依据。简报及报告使用生成时的数据快照、采价/映射版本和外部信息。“查看关联版本”调用新增的bulletinActions.detail/reportActions.detail只读入口，按objectId与version取得原版本，复用既有详情与引用简报展示。单次操作前后留存与该版本当前完整正文分别说明，避免把同版本后续文字编辑当成当时操作内容。未向商户加载追溯或管理数据脚本。

T10完成时，check-trace及既有8项数据检查全部通过；check.mjs检查55个文件和11个路由通过。专项验证双端HTML实际脚本加载顺序和11个路由都有实现，以及只读汇总、稳定ID、日期查询、来源变更、旧版定位、发布边界和HTML转义。README补充完整演示顺序；仍未进行浏览器、截图、布局及渲染字号验证。

## 当前交付边界

T01—T10静态实现及代码检查全部完成，没有待设计的既定模块。后续按用户具体反馈调整，不自动增加功能。没有真实接口、服务器、Git仓库或部署；缓存与消息仅用于演示，浏览器交互和视觉验收仍未开展。

## 新会话起点

在 Codex 中把 `E:\Cursor\frozen-food-ai-prototype` 添加为本地项目，然后在该项目下新建会话。推荐首条消息：

> 请先读取本项目 AGENTS.md、README.md、docs/需求与页面映射.md 和 docs/接续说明.md，理解现有双端框架和公共样式。接下来先设计【填写本次模块】，只完成该模块，不扩展其他功能；默认只做静态检查。

若尚未确定模块，请先与用户确定本次范围，不自动铺开所有页面。用户要求“先分析”时只分析。

## 扩展方式

1. `assets/js/config.js` 是双端导航的单一来源，现有 hash 路由可直接书签和返回。
2. 后续页面在 `assets/js/pages.js` 中通过 `FrozenApp.pages['admin:prices'] = function (container, route) { ... }` 方式注册；键使用 `端:路由ID`。
3. 复杂模块可拆独立脚本，并在对应端 HTML 中于 `shell.js` 之前加载；复用公共样式和函数。
4. 无注册页面时，公共框架自动显示占位，不冒充已完成功能。商户端与管理端只加载当前端路由。
5. 完成指定模块后更新本文的“已完成/尚未完成”和相关需求映射状态。

## 校验记录

创建时按静态规则校验，结果在 `docs/静态检查记录.md`。未进行浏览器渲染、截图字号对照或点击验证，不将静态检查称为视觉验收。

T01 完成后 `scripts/check.mjs` 检查通过：19 个文件、11 个路由定义，具体范围见静态检查记录。仍未启动服务或进行浏览器验证。

T02 完成后运行 `scripts/check.mjs` 及 `scripts/check-prices.mjs`。后者为 Node 数据逻辑回归检查，覆盖 CSV 边界、整批导入、重复校验、复核及快照保留；不属于浏览器交互验收。

T03 完成后 `scripts/check.mjs` 与 `scripts/check-categories.mjs` 通过，覆盖公共品类引用、组合条件、未映射隔离、保存校验、数量归组及当前采价关联。未启动浏览器，未进行布局、弹窗或字号渲染验证。

T04 完成后 `scripts/check.mjs` 与 `scripts/check-sources.mjs` 通过，覆盖重复来源校验、来源/信息状态分离、范围和版本匹配、重新核验及历史保留；未启动浏览器。

T05 完成后 `scripts/check.mjs` 与 `scripts/check-analysis.mjs` 通过，覆盖96条数量的连续性、期间聚合、基期与缺失数据、当前映射/采价复核/来源状态联动。未运行浏览器或字号渲染验证。

T06 完成后 `scripts/check.mjs` 与 `scripts/check-bulletins.mjs` 通过，覆盖生成与发布门槛、修订可见性、源数据变化和返回副本不改写旧快照、自然月边界/加权价格/月底库存、引用核验过滤、公开投影、本地保存与失败降级、文本转义和 CSS 令牌引用。未启动服务或浏览器，未验证实际 iframe 消息、弹窗操作和渲染字号。
